<div class="panel panel-container">
      <table class="table table-bordered">
        <thead>
          <tr>
            <th>Author</th>
            <th>Post Title</th>
            <th colspan="3">Actions</th>
          </tr>
        </thead>

        <tbody>
          <% posts.each do |post| %>
            <tr>
              <td><%= post.user.email %></td>
              <td><%= post.title %></td>
              <td><%= link_to 'Show post content', admin_post_path(post.id), class: 'btn btn-warning' %></td>
              <% if post.status == 'New' %>
                <td><%= button_to "Approve", post_path(post.id), method: :patch, params: { post: { status: 'Approved' } }, class: 'btn btn-primary'%></td>
                <td><%= button_to "Decline", post_path(post.id), method: :patch, params: { post: {status: 'Declined' } }, class: 'btn btn-danger'%></td>
              <% else %>
                <td><%= link_to 'Edit post', edit_admin_post_path(post.id), class: 'btn btn-primary' %></td>
                <td><%= link_to 'Delete post', admin_post_path(post.id), method: :delete, data: { confirm: 'Are you sure you want to delete this user?' }, class: 'btn btn-danger', remote: true %></td>
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>
		</div>
