jQuery('.like-container').html("<%= escape_javascript render(partial: 'like_btn', locals: { post: @post }) %>")
jQuery('.like_show').html("<%= @post.likes.count == 0 ? 'Like: ' : @post.likes.count == 1 ? 'Like: ' : 'Likes: ' %> <%= @post.likes.count %>")
